// tsconfig.jest.json
{
    "extends": "./tsconfig.json",
    "compilerOptions": {
      // Switch to React’s automatic JSX runtime so ts-jest can emit valid JS:
      "jsx": "react-jsx"
    },
    "include": ["**/*.ts", "**/*.tsx", "__tests__/**/*"]
  }
  